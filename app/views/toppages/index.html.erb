<% if logged_in? %>
  <p>User: <%= current_user.name %></p>
<% else %>
  <div class="center jumbotron">
    <div class="text-center">
      <h1>Welcome to the RandomTag</h1>
      <%= link_to 'Sign up now!', signup_path, class: 'btn btn-lg btn-dark' %>
    </div>
  </div>
<% end %>

